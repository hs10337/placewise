# Product presentation

Source: `docs/product.md`. Visual authority: `docs/brand_guidelines.md` and existing semantic tokens. Direct build within the approved brand. Refero live research returned NO_SUBSCRIPTION; bundled typography, craft-details and visual-workflow references inform hierarchy, readable measures, focus and responsive QA.

Reference lock: white reading canvas, faint Glacier section surfaces, Oxblood headlines and actions, upright Bricolage 600 display type, Instrument Sans reading type, 8px spacing rhythm. Preserve an open composition with one dominant subject per chapter. Use the existing illustrated Telegraph Hill map as a clearly labeled prepared example, with the existing place questions and source links. No new documentary imagery or fabricated product evidence.

Decision ledger: the brand owns typography, palette, restrained borders and connected place/question context. The PRD owns audience, two entry paths, trust rules, global selection with honest fallbacks, phase boundaries and proposed performance targets. Bundled craft guidance owns balanced headlines, short reading measures, semantic navigation, focus states and reduced motion. The presentation brief owns chapter pacing, scroll progress and direct chapter navigation. No forced scroll snapping or animation-dependent content.

Eight chapters: idea, problem, people, experience, trust, scope, foundation, validation. Condense the PRD for a short talk; label plans, proposed targets and the prepared example. Download the exact canonical PRD through a raw import, avoiding the stale public copy.

Navigation revision: moved all eight chapter links into the existing workspace drawer beneath the Product view selector. The presentation uses full width with no persistent chapter rail. Drawer links close the dialog, scroll to the selected chapter and transfer focus; the drawer highlight and bottom progress controls share the scroll-derived active chapter. Verified desktop layout, active highlight, click selection, and keyboard selection at 390px with no horizontal overflow. TypeScript and the production build pass in the existing local dependency copy.

## Verification, September 24

Passed TypeScript and the foundation production build in the existing local dependency copy at `/private/tmp/placewise-ios-check.R52WAN`. Repository dependencies remain cloud-offloaded; the repository build stalled and was stopped. Synced the four changed application files into that existing port-5178 preview. The temporary component resolves its raw PRD import to the canonical repository file using an absolute path, while repository source retains its portable relative import. Vite's existing large shared-chunk warning remains.

Visually checked desktop 1214×964, mobile 390×844 and narrow 320×740 layouts, including dark appearance. Verified chapter jumps and progress, first/last disabled controls, direct chapter links, selected map question and answer consistency, full-brief download metadata, loaded brand fonts and no horizontal overflow. Corrected clipped map markers at 320px and rechecked bounds. No browser console errors observed. Restored system appearance and the normal viewport, and left the presentation at its opening chapter. Reduced-motion CSS keeps all content available with transitions disabled. Real-device Safari and printing were not tested.

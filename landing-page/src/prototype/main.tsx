import React from 'react'
import ReactDOM from 'react-dom/client'
import '../lib/ios-components'
import FoundationPreview from './FoundationPreview'
import './foundation.css'

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><FoundationPreview /></React.StrictMode>)

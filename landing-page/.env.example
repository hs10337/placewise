# Copy to .env.local and add a public access token from your Mapbox account.
# Allow the preview origin in its URL restrictions. Never use a secret token here.
# Rebuild with npm run build:studio after setting this value.
VITE_MAPBOX_ACCESS_TOKEN=
